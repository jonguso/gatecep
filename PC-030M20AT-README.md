# PC-030M20AT — Floating Coach G Discovery → Summary → Recommendation

Decision Lab captures the starting idea, then the canonical global Floating Coach G owns the conversation.

Flow: starting idea → Discuss with Coach G → free-text discovery → one useful follow-up at a time → summary → investor confirms/corrects → recommendation → simulation.

The existing authenticated Floating Coach API is called for the recommendation only after summary confirmation. Session facts are temporary scenario context, not Investor DNA changes. No REAL/Practice portfolio, goal, broker, CDSC or execution state is mutated. `Skip discussion → Simulation` remains available for deliberate pure what-if testing. Broker Action Plan remains downstream and advisory-only.
