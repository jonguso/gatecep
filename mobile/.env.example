# Production builds use the verified HTTPS backend by default.
# Set this only when a different deployment is intentionally being tested.
EXPO_PUBLIC_API_URL=https://gatecep-trader-production.up.railway.app
