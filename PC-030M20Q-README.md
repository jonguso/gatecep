# PC-030M20Q — All Filter Defaults

Portfolio Analysis, Portfolio Risk, and Portfolio Rebalancing now open with
their applicable lists set to **All**. Investors can still select a narrower
category afterward.

The responsive panel sizing introduced for mobile screens is preserved.

## Verify

From `mobile`:

```bash
bash scripts/verify-pc030m20q-all-filter-defaults.sh
npx expo start --clear --lan
```
